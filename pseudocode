/*
char *get_next_line(int fd)
{	
	if (it has read more and stored it in buffer)
	{
		store / append everything on a static tempbuff;
	}
	else if (it has read 0 && tempbuff is empty || error of lecture)
	{
		return (NULL);
	}
	
	CHECKING AND MANAGING THE TEMPBUFF

	if (a newline is found in tempbuff)
	{
		take that string;
		remove that string from tempbuff;
		{
			malloc es (bytes - ft_strchr(s, '\n))
		}
		return (that string);
	}
	if (null char is found in tempbuff)
	{
		take that remaining string;
		empty tempbuff;
		return that string;
	}
	else
	{
		
		else 
			do nothing
	}
}
*/